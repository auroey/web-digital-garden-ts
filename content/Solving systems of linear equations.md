---
title: Solving systems of linear equations
date: 2025-08-14
---
判断解的数量：先看是否奇异，然后比$r(A) 和 r(A|b)$

1. **改变方程等号右边的值（常数项）**
    
    - 不改变法向量（即平面方向不变），只平移平面
        
    - 如果原本有唯一交点，可能变成无解（平移破坏交点）或无穷多解（平移到重合）
        
2. **两平面相交 → 一条直线**  
    三个平面共享同一条直线 → 无穷多解（line）
    
3. **两平面重合**  
    如果第三个平面和它们的交集是直线 → 无穷多解（line）  
    如果第三个平面也重合 → 无穷多解（plane 或 all space）
    
4. **秩判定**（Rouché–Capelli 定理）
    
    - $r(A) = r(A|b) = 3$ → 唯一解
        
    - $r(A) = r(A|b) < 3$ → 无穷多解
        
    - $r(A) < r(A|b)$ → 无解
        

解的过程：使用 [[Gaussian Elimination]]

- 解方程组时，我们会用“消元法”先消去一个变量，再回代求另一个变量。
    
- 如果只取方程的系数（丢掉常数项），这些消元步骤在矩阵上就是**行变换**（row operations）。
    

---

行阶梯形矩阵 (Row Echelon Form, REF) 特征

在**REF**中：

1. 每行的第一个非零元（称为**主元 pivot**）都在上一行主元的右边。
    
2. 主元下面全是 $0$。
    
3. 主元可以不一定是 $1$（但常会用倍乘让它是 $1$）。
    
4. 在主元右边的元素任意，但在全零行右边不能再有非零。
    

即，长度不规律的楼梯

---

简化行阶梯形矩阵 (RREF)

在 RREF 里，REF 的条件基础上还要满足：

- 每个主元是 $1$。
    
- 每个主元所在列其他位置都是 $0$（不仅下面是 $0$，上面也要是 $0$）。
    

即，只有对角线非零且为1

从 REF 得到 RREF：

1. 如果主元不是 1，就把这一行除以主元值，让它变成 1。
    
2. 用这一行去**消掉主元列中上方的非零数**（之前 REF 只清了下面的，现在要把上面的也清掉）。
    
3. 逐列重复，直到所有主元列都干净。


- **唯一性**：同一个矩阵的 RREF 是唯一的，但 REF 不唯一。
    
- **秩 = 主元数**（和 REF 一样）。
    
- **解的形式直观**：RREF 实际上就是解线性方程组的“最终状态”，直接能读出解。

---

### **4. REF 对解的提示**

设：
- $r_A$ = 系数矩阵 $A$ 的秩
- $r_{[A|b]}$ = 增广矩阵的秩
- $n$ = 未知数个数

---

**Step 1：判断是否矛盾**
- 如果 $r_A < r_{[A|b]}$  
  → 输出 **无解**（矛盾行：$[0\ 0\ \dots\ |\ c],\ c \neq 0$）

![image.png](https://kennels.oss-cn-beijing.aliyuncs.com/img_for_typora/20250814190832.png)

---

**Step 2：若不矛盾（$r_A = r_{[A|b]}$）**
- 如果 $r_A = n$  
  → 输出 **唯一解**（无自由变量）
![image.png](https://kennels.oss-cn-beijing.aliyuncs.com/img_for_typora/20250814190852.png)

- 否则（$r_A < n$）  
  → 输出 **无穷多解**（有自由变量）
![image.png](https://kennels.oss-cn-beijing.aliyuncs.com/img_for_typora/20250814190845.png)
n是列数（一般 A 都是方阵）


