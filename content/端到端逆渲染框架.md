---
title: 端到端逆渲染框架
date: 2025-08-14
---
- 输入：一张室内图像。
    
- 模块：
    
    1. **MGNet**：预测材质与几何（反照率、粗糙度、金属度 + 深度、法线等）。
        
    2. **LightNet**（三个子模块）：
        
        - ResNet34 提取局部特征（参考 PixelNeRF）；
            
        - SSRT 模块计算交点光源位置；
            
        - MLP 预测光照值。
            
    3. **可导 Monte Carlo 渲染器**：用来复现图像外观，支持训练中反向传播。



这一页是论文最核心的内容，详述了作者提出的**端到端逆渲染框架**的完整流程和每个模块的作用。可以总结为以下四部分：

---

### 🔧 1. 总体流程（见 Figure 3 左侧图）

论文的逆渲染系统包含五步：

1. **MGNet**：输入图像 → 预测材质和几何（深度、法线、反照率、金属度、粗糙度）
    
2. **BRDF 重要性采样**：根据预测结果，选择关键的入射光方向集合 $d_i$
    
3. **SSRT 光线追踪**：从点 $p$ 沿 $-d_i$ 方向进行屏幕空间光线追踪，找到光源位置 $s$，并提取该处的图像特征
    
4. **LightNet**：利用特征、视角方向、G-Buffer，预测该方向上的入射光照 $L_i(p,d)$
    
5. **可微蒙特卡洛积分**：结合所有 $L_i$，通过积分重建图像 $\hat{I}$，与原图计算损失并反向传播
    

---

### 🧠 2. MGNet：材质-几何网络（§4.1）

- 输入：HDR图像（或LDR图像预处理后）
    
- 架构：DenseNet121 编码器 + 4个解码器
    
- 输出：
    
    - Albedo（反照率 $A$）
        
    - Normal（法线 $N$）
        
    - Depth（深度 $D$）
        
    - Material（材质 $M$）含 Roughness ($R$) 与 Metallic ($M_t$)
        

---

### 💡 3. LightNet：光照预测网络（§4.2）

- 使用 ResNet34 编码器提取图像特征 $F$
    
- 对每个像素点 $p$，选择一个入射方向 $d$
    
- 执行光线追踪：$s = trace(p, -d)$
    
- 提取 $s$ 处的特征、法线、粗糙度等（来自 G-buffer）
    
- 利用 MLP 解码器 $f$，预测该方向上的光照：
    
    Li(p,d)=f(γ(d),F[π(s)],G[π(s)])L_i(p, d) = f(\gamma(d), F[\pi(s)], G[\pi(s)])
    
    > 其中 $\gamma$ 是 NeRF 中常用的位置编码函数，$\pi(s)$ 是将3D点 $s$ 投影到图像上的位置。
    

---

### 🌌 4. NeRF 风格的视野外光照网络（§4.3）

- SSRT 的问题：光线可能射出图像边界，获取不到信息
    
- 解决方法：
    
    - 使用 NeRF 形式建模图像视野外区域
        
    - 利用 **HyperNetwork** 从图像提取全局特征 $F_g$
        
    - 输出 NeRF MLP 的权重 $\Phi$
        
    - 再用 NeRF 的体渲染方式预测背景照明
        

---

### ✅ 总结：

> 本页详细讲述了论文提出的可导逆渲染框架，包括：
> 
> - 如何预测几何材质（MGNet）
>     
> - 如何采样并追踪入射光（BRDF + SSRT）
>     
> - 如何估计图像内和图像外的光照（LightNet + NeRF）
>     
> - 如何用可微渲染重建图像，并与真实图像比对以反向传播
>     

整套系统可以端到端训练，学会从图像中恢复物理世界的隐变量。是当前逆渲染领域极具代表性的方案之一。需要我再帮你画一个流程图或者模块结构图说明吗？