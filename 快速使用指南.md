# 🚀 快速使用指南 - 180分钟自动同步

## ✅ 当前状态

您的自动同步已经成功启动，设置为**每180分钟（3小时）**自动同步一次。

## 📋 常用命令

### 查看状态
```bash
./status-sync-daemon.sh
```

### 手动同步（立即执行）
```bash
./test-sync.sh
```

### 停止自动同步
```bash
./stop-sync-daemon.sh
```

### 重新启动自动同步
```bash
./start-sync-daemon.sh
```

## ⏰ 同步时间表

- **当前启动时间**：21:29
- **下次自动同步**：约 00:29（3小时后）
- **同步间隔**：180分钟（3小时）

## 📝 日志文件

- **守护进程日志**：`sync-daemon.log`
- **同步操作日志**：`sync.log`

## 🔍 实时监控

```bash
# 监控守护进程日志
tail -f sync-daemon.log

# 监控同步操作日志
tail -f sync.log
```

## 💡 使用建议

1. **日常使用**：无需任何操作，系统会自动同步
2. **紧急同步**：运行 `./test-sync.sh` 立即同步
3. **状态检查**：运行 `./status-sync-daemon.sh` 查看运行状态
4. **重启同步**：如果遇到问题，先停止再启动

## 🎯 优势

- ✅ 无需 sudo 权限
- ✅ 完全自动化
- ✅ 可随时手动控制
- ✅ 详细日志记录
- ✅ 进程状态监控

---

**最后更新**：2025-01-17  
**同步间隔**：180分钟  
**状态**：✅ 正在运行
